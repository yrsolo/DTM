# Script Archive

Historical helper scripts removed from the active `scripts/` surface.

- `invoke_cloud_timer.cmd` was retired after the live function smoke moved into grouped `agent/smokes/*` flows and the wrapper stopped participating in active operator or CI scenarios.
