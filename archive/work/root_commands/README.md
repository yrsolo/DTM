# Root Command Archive

Historical root-level helper commands that no longer belong to the active repo surface.

- `run_alert_eval_ci.cmd` was retired after the reminder evaluator moved under `agent/reminders/` and the wrapper stopped participating in live CI/runtime flows.
