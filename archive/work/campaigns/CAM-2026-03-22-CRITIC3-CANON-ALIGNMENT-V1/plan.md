# CAM-2026-03-22-CRITIC3-CANON-ALIGNMENT-V1

## Goal

Close the last high-visibility alignment gaps called out by `critic3`:

- make `access_api` read as the unambiguous owner of the primary browser read model,
- make active docs and the active `src` map tell the same calm story.

## Scope

1. Rename the main `access_api` surface around one explicit primary read-model term.
2. Update the top HTTP router and nearby docs to use that same term.
3. Align README and architecture/module docs around one active code-map narrative.

## Non-goals

- no new architecture tree reduction,
- no bootstrap wave,
- no further `attachments`/`snapshot` boundary changes.
