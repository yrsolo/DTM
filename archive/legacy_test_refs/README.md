# Legacy Test Refs

These files are archived reference tests only.

- They are not part of the supported default test contour.
- They are kept only as readable examples of historical legacy behavior.
- File names intentionally do not match `test*.py`, so default discovery does not pick them up.
