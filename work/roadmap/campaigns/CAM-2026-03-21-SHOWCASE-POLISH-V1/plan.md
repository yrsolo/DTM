# CAM-2026-03-21-SHOWCASE-POLISH-V1 Plan

Smell:
- the repo reads cleaner inside than it presents itself from the top-level entry docs

Target ideal:
- a reader lands on the repo and immediately sees the current product story, the current architecture canon, and the shortest active reading path without tripping over superseded recovery waves

Kill criteria:
- root README points readers to the active canon and active product/runtime story instead of historical architecture waves
- top-level docs entry pages foreground the current module-first contour and keep deferred/future material out of the default start path
- the first docs hop feels like a showcase entrypoint, not an internal archaeology map

Scope boundary:
- root README and top-level docs entry pages only
- no runtime or API behavior changes

Non-goals:
- no archive rewrite
- no new architecture campaign
