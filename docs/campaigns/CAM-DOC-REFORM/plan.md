# CAM-DOC-REFORM Plan

## P01 - CAM system and doc roots cleanup
- `CAM-DOC-REFORM-P01-T001` define CAM-P-T rules and templates.
- `CAM-DOC-REFORM-P01-T002` create campaign shell for documentation reform.
- `CAM-DOC-REFORM-P01-T003` build target active `docs/` structure and single map.
- `CAM-DOC-REFORM-P01-T004` archive legacy `doc/` tree via `git mv` and create deprecation stub.

## P02 - Migration docs normalization and agile cleanup
- `CAM-DOC-REFORM-P02-T001` keep one active DB migration plan/evidence path; archive conflicting migration drafts.
- `CAM-DOC-REFORM-P02-T002` reduce `agile/` to active control-plane set and archive legacy content.
- `CAM-DOC-REFORM-P02-T003` refresh root README entrypoints and remove active references to deprecated `doc/*`.

